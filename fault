现在有个问题，为什么50/s延迟这么大”schedule_delay_p95_ms":13357.815，最大的延迟有13ms，是本地性能的原因还是什么，
有没有提升的空间。因为延迟大的话实际可能没有达到每秒发50次的效果







先纠正两个点：

```text
schedule_delay_p95_ms = 13357.815 ms
```

这是约 `13.36秒`，不是13毫秒；而且它是 P95，不是最大值。本轮最大调度延迟约为：

```text
14025.333 ms ≈ 14.03秒
```

结论：这轮确实没有稳定达到50次/秒，而且同时存在本地线程池排队和阿里云限流，其中阿里云限流是成功吞吐量上不去的主要原因。

## 实际发压速度

本轮数据：

```text
目标：50次/秒 × 60秒
请求数：3000
实际开始请求的时间跨度：约69.963秒
实际平均发起速率：约42.866次/秒
```

计算：

```text
(3000 - 1) ÷ 69.963
≈ 42.866次/秒
```

所以没有达到目标50次/秒。

本地调度情况：

```text
schedule delay P50：约0.947ms
schedule delay P95：约13.358秒
schedule delay 最大值：约14.025秒

延迟超过1秒的请求：859个
延迟超过5秒的请求：859个
```

前半部分调度正常，后面线程池被占满，部分请求开始排队。

## 阿里云侧存在明确限流

失败日志中有2342次相同错误：

```text
429 ResourceExhausted
Scaling limit exceeded
burst: 300
refill rate: 5/s
```

它表示当前账号/地域/模板的扩容限制大致为：

```text
允许短时间突发：300个
突发额度消耗后恢复速度：5个/秒
```

但你的目标是持续：

```text
50个/秒
```

当前恢复能力只有：

```text
5个/秒
```

因此，即使本地程序能够完美地按照50次/秒发请求，后续大量请求也会被阿里云以429拒绝。

本轮最终结果：

```text
总请求：3000
API成功：621
API失败：2379
其中429限流：2342
RemoteProtocolError：37
API成功率：20.7%
```

这与“突发300 + 后续约5次/秒恢复”的限制基本吻合。

## 本地为什么也出现排队

当前脚本默认：

```text
--max-workers 256
```

每个线程需要完成完整流程才释放：

```text
create
→ 第一条命令
→ 第二条命令
→ kill
→ 释放线程
```

成功请求的平均阶段耗时约为：

```text
API创建：2.226秒
第一条命令：2.681秒
第二条命令：0.411秒
合计：约5.318秒，还未计算kill
```

按照50次/秒估算需要的在途并发：

```text
50 × 5.318
≈ 266个线程
```

这已经超过默认256个线程，计入长尾和清理耗时后，256更不够，因此后面的请求会在本地排队。

## 有没有提升空间

有，但处理顺序很重要。

### 1. 先申请提升阿里云限流

需要把错误信息发给主管或阿里云项目人员：

```text
当前云沙箱压测目标是持续50次/秒。
服务返回：
429 ResourceExhausted
Scaling limit exceeded
burst: 300
refill rate: 5/s

请协助把该账号、地域和模板的Sandbox扩容/创建限流提高到
至少持续50次/秒，并确认相应的Sandbox实例并发配额。
```

如果不提高 `refill rate: 5/s`，只优化本地程序不会提高成功吞吐量，只会更快地产生429。

### 2. 限流提升后，再增加本地线程数

可以先测试512：

```powershell
python benchmark.py run --rates 50 --duration-seconds 60 --storages none --max-workers 512 --confirm
```

然后检查：

```text
schedule_delay_p95_ms
api_success_rate_pct
ready_success_rate_pct
```

目标是：

- `schedule_delay_p95_ms` 尽量保持在几十毫秒以内；
- API 成功率接近100%；
- 实际发起速率接近50次/秒。

在阿里云限流没有提升前，不建议直接增加线程数，因为它只会产生更多429。

### 3. 更高档位需要更专业的发压方式

对于100/s、200/s持续60秒：

- 512个同步线程可能仍然不够；
- Windows 大量线程会增加上下文切换；
- 本地公网网络也可能成为瓶颈；
- 更适合改成异步请求模型，或者使用多台压测机；
- 最好在与云沙箱同地域的 ECS 上发压，减少公网网络波动。

总结：

```text
schedule_delay过大：
部分原因是本地max_workers=256不足。

成功率只有20.7%：
主要原因是阿里云明确限制为burst 300、refill 5/s。

当前实际发起速度：
约42.866次/秒，没有稳定达到目标50次/秒。

正确优化顺序：
先提升阿里云限流和实例配额
→ 再把max_workers提高到512
→ 再检查schedule_delay和成功率
→ 100/s、200/s考虑异步或分布式压测
```