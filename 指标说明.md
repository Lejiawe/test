# 云沙箱启动并发速度测试指标说明

本项目支持两种驱动：阿里云使用 `e2b`，字节/火山使用
`volcengine_native`。两者共享相同的调度、统计和结果文件格式，但功能就绪
里程碑的判定方式不同。

## 时间点定义

阿里云：

```text
T0：开始调用 Sandbox.create()
T1：Sandbox.create() 返回 Sandbox ID
T2：第一条命令执行完成
T3：第二条命令执行完成
```

字节/火山原生：

```text
T0：开始调用 CreateSandbox
T1：CreateSandbox 返回 Sandbox ID
T2：DescribeSandbox 首次返回 Ready
T3：DescribeSandbox 第二次确认 Ready
```

统一计算口径：

```text
api_latency_ms = T1 - T0
first_command_latency_ms = T2 - T0
second_command_latency_ms = T3 - T0
```

三项均从创建开始累计，正常成功样本应满足：
`api_latency <= first_command_latency <= second_command_latency`。

## 统计口径

- `min`/`max`：有效样本的最小值和最大值。
- `mean`：有效样本算术平均值。
- `P50`/`P90`/`P95`/`P99`：对应百分位延迟。
- `exclude_first_from_mean = true` 时，每个TPS档位的第一个沙箱不参与
  `mean`，但仍参与 `min`、`max`、各百分位和成功率统计。
- 延迟单位均为毫秒，已经是样本统计结果，不需要再除以请求数或持续时间。

## 成功率

- `api_success_rate_pct`：创建成功且获得 Sandbox ID 的比例。
- `ready_success_rate_pct`：阿里云两条命令均成功，或火山原生连续两次查询均为
  Ready 的比例。
- `cleanup_success_rate_pct`：已创建的 Sandbox 成功清理的比例。

## 调度指标

`schedule_delay_p95_ms` 表示95%的任务相对计划发起时间的本地调度延误不超过
该值。它主要用于判断压测机是否真正按目标TPS发出请求，不等同于云端创建延迟。

逐字段定义见 `指标说明.csv`，完整使用流程见 `README.md`。
