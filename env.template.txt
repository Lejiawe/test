# 将本文件复制为 .env 后再填写真实值。
# 本模板可以提交到 GitHub；不要在本文件中填写真实 API Key。

# 必填：三者必须属于同一阿里云地域。
E2B_API_KEY=e2b_xxx
E2B_API_URL=https://api.cn-beijing.e2b.fc.aliyuncs.com
E2B_DOMAIN=cn-beijing.e2b.fc.aliyuncs.com

# 官方模板及单个 Sandbox 的存活超时（秒）。
E2B_TEMPLATE=code-interpreter-v1
E2B_SANDBOX_TIMEOUT=300

# 无挂载测试不需要填写以下配置。

# 可选：所有场景共用的 VPC 配置。
# E2B_VPC_CONFIG_JSON={"vpcId":"vpc-xxx","securityGroupId":"sg-xxx","vSwitchIds":["vsw-xxx"]}
E2B_VPC_CONFIG_JSON=

# OSS 测试：Bucket 名称、Endpoint 和 RAM 角色必须替换为真实值。
# E2B_OSS_CONFIG_JSON={"mountPoints":[{"bucketName":"your-bucket","mountDir":"/mnt/oss","bucketPath":"/","endpoint":"http://oss-cn-beijing-internal.aliyuncs.com","readOnly":false}]}
E2B_OSS_CONFIG_JSON=
E2B_ROLE_ARN=

# NAS 测试：metadata key 和 JSON 格式必须由阿里云项目方明确提供。
# 不要自行猜测 E2B_FILE_METADATA_KEY。
E2B_FILE_METADATA_KEY=
E2B_FILE_CONFIG_JSON=

# 可选：合并到每次 Sandbox.create(metadata=...) 的额外字符串键值。
# E2B_EXTRA_METADATA_JSON={"project":"startup-benchmark"}
E2B_EXTRA_METADATA_JSON=
